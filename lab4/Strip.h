#define MAX_RESISTORS_PER_STRIP 40#define MAX_STRIP_NUMBER  10class Strip {  private:      int             number;      Resistor        resistors[MAX_RESISTORS_PER_STRIP];   public:        Strip ();        ~Strip ();        bool         addResistor (double resistance,                            char* name, int leftStrip,                            int rightStrip);        void         print ();};